import React, { useState } from 'react'
import { TextInput, View } from 'react-native'

function Edit() {
    const [data, setData]=useState()
  return (
    <View>
        {/* <TextInput 
        value={id} onChangeText={}/>
        <TextInput/> */}
    </View>
  )
}

export default Edit